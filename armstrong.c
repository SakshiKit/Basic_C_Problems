#include <stdio.h>
#include <math.h>

int main() {
 int a,b,c;
 printf("enter the value of a, b, c \n");
 scanf("%d, %d, %d", &a, &b, &c);

 if(pow(a,c)+pow(b,c)+pow(c,c)= (a,b,c)){
    printf("the number is armstrong: \n");
 }

 else {
    printf("the number is not armstrong:\n");
 }
 return 0;

}