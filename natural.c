#include<stdio.h>
#include<math.h>

int main(){
    int n;
    printf("enter the number n:");
    scanf("%d", &n);

    if(n>=0){
        printf("n is a nautural number");
    }
     else{
        printf("n is not a natural number");
     }

  return 0;
}