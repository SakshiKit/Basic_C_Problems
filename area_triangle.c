#include<stdio.h>

int main() {
    int base, height;
    printf("enter the base");
    scanf("%d", &base);
     printf("enter the height");
    scanf("%d", &height);
    printf("area of triangle is: %d", 1 / 2 * base * height);
    return 0;
}